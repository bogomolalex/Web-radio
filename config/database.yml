# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  database: radio_development
  host: localhost
  username: root
  password: alexandr
  encoding: utf8
# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  database: radio_test
  host: localhost
  username: root
  password: alexandr
  encoding: utf8

production:
  adapter: mysql
  database: radio_production
  host: localhost
  username: root
  password: lebeda123456
  encoding: utf8
